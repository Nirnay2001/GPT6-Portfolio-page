apiVersion: apps/v1
kind: service
metadata:
  name: portfolio-service
  labels:
    app: portfolio
  spec:
    type: nodePort
    selector:
      app: portfolio 
    ports:
      - protocol: TCP
        port: 80
        targetPort: 80
        nodePort: 30206