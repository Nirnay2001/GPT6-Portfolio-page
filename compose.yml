services:
  web:
    image: nirnay2001/gpt6-portfolio-page:latest
    volumes:
      - mydata:/mydata
    ports:
      - "80:80"
    networks:
      - app-net1

volumes:
  mydata:
    name: mydata

networks:
  app-net1:
    driver: bridge